import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iteridea.egm',
  appName: 'egm-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
